## 1. Update CharacterIdentity component

- [x] 1.1 Add `list-disc pl-4` to the traits `<ul>` in `src/components/CharacterIdentity.tsx`
- [x] 1.2 Change the trait name `<span>` class from `font-medium` to `font-bold`

## 2. Verify

- [x] 2.1 Run the dev server and confirm each kindred's traits render as a bulleted list with bold names
